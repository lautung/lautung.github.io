---
title: 分层架构
description: UI 层、数据层、领域层的职责和依赖方向。
---

# 分层架构

分层架构让代码按职责组织，降低页面、业务逻辑和数据来源之间的耦合。

## 常见层次

- UI 层：显示状态、处理交互。
- Domain 层：可选，封装业务规则。
- Data 层：统一访问网络、本地数据库和缓存。

## 依赖方向

UI 层依赖领域层或数据层接口，数据层隐藏具体实现。越靠近 UI，越容易变化；越靠近领域，越应该稳定。
